#include <stdio.h>
#include <math.h>
#include <stdlib.h>
int main()
{       
    double a;
    scanf("%lf",&a);
    int b=ceil(a);
    // int b=floor(a);
    // int b=round(a);
    //  int b = sqrt(a);
    // int b = pow(number,power);
    // int b = sqrt(a);
    //  int b = abs(a);
    printf("%d",b);
    return 0;
}