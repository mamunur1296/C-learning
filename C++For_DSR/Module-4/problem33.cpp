#include <iostream>
#include <algorithm>
#include <string>
using namespace std;

int main() {
    char ch[1000001];

    while (cin >> ch) {
        int n=strlen(ch);
        cout << ch;
    }

    return 0;
}
