#include <iostream>
using  namespace std;
int main()
{
    int a;
    char b;
    cin >> a >> b ;
    cout << a << " "<< b << endl; // normal output . 
    cout << int(a) << " "<< (long long int )(b) << endl; // int(a) is call typecusting . ok , and multipoll variable name hola (long long int )(b) ai vaba use korta hoy . 
    return 0;
}